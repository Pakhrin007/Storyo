import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.white;
  static const secondary = Color(0xFF0B1220);
  static const accent = Color(0xFF2F6BFF); // optional blue accent
}
